<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Shizuku permissions -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <application
        android:name=".ControlledApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="ZYYH被控端"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZYYHControlled">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Shizuku provider -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- MQTT service -->
        <service android:name="org.eclipse.paho.android.service.MqttService" />

        <!-- Shizuku UserService for shell commands -->
        <service
            android:name=".ShellService"
            android:permission="rikka.shizuku.self.permission.IS_SELF"
            android:process=":shizuku" />
    </application>
</manifest>
